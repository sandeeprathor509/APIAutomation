version: 2.1
jobs:
  API-Automation:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout

      - run:
          name: Build
          command: mvn clean

      - run:
          name: Test
          command: mvn test

workflows:
  sample: 
    jobs:
      - API-Automation

